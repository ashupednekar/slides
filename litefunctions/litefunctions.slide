# LiteFunctions


What - serverless on your cluster: Lambda-shaped, without the vendor
Why  - building from first principles — cuz we can 😉



## Hello 👋🏻

.html hello.html

## buzzword - Serverless

<br><br>

.html serverless.html

## So we want self hosted lambda

<br>

.html selfhosted.html

## So what's infra, in the most generic sense

<br>

.html infra.html

## Let's log in to see what it's got

.html demo-login.html

## Let's write an API

.html demo-writeapi.html

## There you go

.html demo-there.html

## How about some websockets

.html demo-websockets.html

## How does it work

.html how-does-it-work.html

## Lambda Architecture

Cloud serverless: client → managed gateway → black-box functions. Great until you need the knobs they won’t give you.

One platform’s opinions: languages, limits, cold starts, bill surprises.

## LiteFunctions Architecture

Your client → Ingestor → Operator → Runtimes. Same rough shape as “functions behind a front door,” but **you** see every hop.

Git tells the system what to run; the cluster runs it. No mystery layer you can’t inspect.

## VCS Integration

Push to repo triggers webhook. Ingestor notifies Operator. Runtime refreshes from VCS. No rebuilds.

## Scenarios - Static vs Dynamic

Static: Go/Rust. Compiled, containerized, rebuild required. Dynamic: Python/TS/Lua. Git pull, no rebuild.

## Scenarios - Sync vs Async

Sync: HTTP. Immediate response. Request/response pattern. Async: NATS pub/sub. Event-driven. Background processing.

## Where does go come in

Go powers the operator, ingestor, and runtime. The engine room of the platform.

## The Goth Stack

Go + Templ + HTMX. Minimal dependencies. Fast iteration. Production ready.

## Kubernetes Operator

Manages Function CRDs. Reconciles deployments. Keeps runtimes alive. Kubebuilder + Cobra.

## Ingestor

Routes HTTP requests. Publishes to NATS. Handles sync + async. Entry point for all traffic.

## Runtimes

Go, Rust, Python, TS, Lua. Multi-language execution. Dynamic refresh for non-static langs.

## SQLc

Type-safe SQL queries in Go. Generated code, no runtime overhead.

## WebAuthn

Passwordless authentication. Hardware security keys. FIDO2/WebAuthn standard.

## What needs to change

Managed serverless trades convenience for **their** defaults. LiteFunctions trades a little DIY for **your** defaults.

Own the control plane. Own the languages. Own the deploy story.

## Summary

Serverless **you** run: multi-language, sync + async, Git-driven, Kubernetes-native.

Not “no servers” — **your** servers, **your** rules. Full control, no vendor lock-in.

## Links
- Code: https://github.com/ashupednekar/litefunctions
- Docs & site: https://ashupednekar.github.io/litefunctions/
- Blog — let’s build our own lambda FaaS: https://ashupednekar.github.io/posts/lets-build-our-own-lambda-faas/

That's it! 😁

note: LiteFunctions is Kubernetes-native serverless — self-hosted FaaS when you want Lambda-shaped ergonomics without handing someone else the keys
